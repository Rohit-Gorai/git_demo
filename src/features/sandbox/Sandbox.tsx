import { useState } from 'react';
import type { SandboxSeed } from '../../types/lesson';
export function Sandbox({seed}:{seed:SandboxSeed}){const [cells,setCells]=useState(seed.cells);const answer=cells.at(-1)?.at(1)||'';const ok=seed.expectedFormula?answer.trim().toLowerCase()===seed.expectedFormula.toLowerCase():answer.length>0;return <section className="card sandbox"><h3>Interactive Sandbox</h3><p>{seed.instructions}</p><table><tbody>{cells.map((row,r)=><tr key={r}>{row.map((cell,c)=><td key={c}>{r===0?<strong>{cell}</strong>:<input aria-label={`Cell ${r+1}, ${c+1}`} value={cell} onChange={e=>setCells(prev=>prev.map((rr,ri)=>rr.map((cc,ci)=>ri===r&&ci===c?e.target.value:cc)))}/>}</td>)}</tr>)}</tbody></table><p className="small">AI tutor: {ok?'Nice work. Your answer follows the lesson pattern.':'Try again. Check whether your formula starts with = and references the right cells.'}</p></section>}
